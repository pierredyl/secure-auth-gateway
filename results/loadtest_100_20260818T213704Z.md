# Load test — concurrency 100

| | |
|---|---|
| Run started (UTC) | 2026-08-18T21:37:39Z |
| Concurrency | 100 |
| Target | `/api/v1/health` via `http://nginx:80` |
| Measured window | 15s (5s warmup discarded) |
| Load model | closed loop, 100 virtual users, think=0s |
| Per-request timeout | 10s |
| Phase reported | `health` |
| Cores under test | 0-7 (8 allocated) |

## Summary

| Concurrency | Throughput (req/s) | p99 latency | Error rate | Total requests | Failed requests |
|---|---|---|---|---|---|
| 100 | 5,934.0 | 72.4ms | 0.0000% | 89,010 | 0 |

Throughput counts every request that completed inside the measured window; 5,934.0 req/s of that answered 200. p99 is over **all** requests, failures included.

## Tail latency

| Distribution | n | min | p50 | p95 | p99 | p99.9 | max | mean |
|---|---|---|---|---|---|---|---|---|
| All requests | 89,010 | 0.2ms | 13.2ms | 48.0ms | 72.4ms | 108.0ms | 167.3ms | 16.8ms |
| 200 accepted | 89,010 | 0.2ms | 13.2ms | 48.0ms | 72.4ms | 108.0ms | 167.3ms | 16.8ms |

**All requests** is the honest headline: it includes failed and timed-out requests at their measured latency. Reading the accepted-only row instead caps the tail at the 10s timeout and lets percentiles *improve* under load as the slowest requests drop out of the set.

## Headroom

**Ceiling used: 14,000 req/s — the general throughput ceiling.**

This run drives GET /api/v1/health — the cheap request-handling path through nginx and the three Go replicas, with no Argon2id hashing and no Postgres round trip. The login-specific ~105 req/s ceiling does not apply, because no login was issued: that ceiling is set by Argon2id saturating all cores, and nothing in this run hashes a password.

| | |
|---|---|
| Achieved | 5,934.0 req/s |
| Ceiling | 14,000 req/s |
| Utilization | **42.4%** |
| 50%-of-capacity target | 7,000 req/s |
| Remaining headroom | 8,066.0 req/s |

> **Within the 50% rule of thumb.** At 5,934.0 req/s this run uses 42.4% of the ceiling, under the 7,000 req/s target, leaving 8,066.0 req/s of headroom.

## Error rate

| | |
|---|---|
| Successful (200) | 89,010 / 89,010 |
| Success rate | **100.0000%** |
| Failed | 0 (0.0000%) |
| — transport errors | 0 |
| — non-200 responses | 0 |
| Availability | no failures in 89,010 requests — bounds availability at >= 4.9 nines, which is the most a sample this size can show |

A request counts as failed if it errored in transport **or** did not answer 200. The two are separated because a 502 and a timeout are both failures with entirely different causes.

**Status codes:** `200`=89,010

## Saturation

Cores `0-7` — 8 cores allocated, so a fully saturated set reads **800%** in docker terms (100% is one busy core). The load generator is pinned outside this set and is excluded.

| Container | avg CPU% | peak CPU% | polls |
|---|---|---|---|
| `secure-auth-gateway-nginx-1` | 226.8 | 291.6 | 60 |
| `secure-auth-gateway-app3-1` | 80.4 | 119.6 | 60 |
| `secure-auth-gateway-app2-1` | 78.4 | 101.0 | 60 |
| `secure-auth-gateway-app1-1` | 76.5 | 111.9 | 60 |
| `secure-auth-gateway-redis-1` | 2.3 | 5.9 | 60 |
| `secure-auth-gateway-postgres-1` | 1.8 | 5.2 | 60 |
| **Combined** | **466.2** | **591.6** | 60 |

| | |
|---|---|
| Saturation of 8 cores (avg) | **58.3%** |
| Saturation of 8 cores (peak) | **73.9%** |
| Polls counted | 60 |
| Effective poll interval | 250ms |

Samples are intersected with the measured window `2026-08-18T21:37:39Z`–`2026-08-18T21:37:54Z`, so saturation covers exactly the seconds the throughput and latency figures came from.

> The cores are far from saturated, so the throughput above is the load that was *offered*, not a capacity limit. Raise `CONCURRENCY` before drawing any conclusion about capacity.

---

_Generated by `cmd/loadreport` from `/report/loadtest_100_20260818T213704Z.json`. See `docs/LOADTEST.md` for how to read this._
