# Load test — concurrency 50

| | |
|---|---|
| Run started (UTC) | 2026-08-19T02:56:33Z |
| Concurrency | 50 |
| Target | `/api/v1/health` via `https://nginx:443` |
| Measured window | 10s (5s warmup discarded) |
| Load model | closed loop, 50 virtual users, think=0s |
| Per-request timeout | 10s |
| Phase reported | `health` |
| Cores under test | 0-7 (8 allocated) |

## Summary

| Concurrency | Throughput (req/s) | p99 latency | Error rate | Total requests | Failed requests |
|---|---|---|---|---|---|
| 50 | 12,125.4 | 23.3ms | 0.0000% | 121,254 | 0 |

Throughput counts every request that completed inside the measured window; 12,125.4 req/s of that answered 200. p99 is over **all** requests, failures included.

## Tail latency

| Distribution | n | min | p50 | p95 | p99 | p99.9 | max | mean |
|---|---|---|---|---|---|---|---|---|
| All requests | 121,254 | 0.2ms | 2.2ms | 14.0ms | 23.3ms | 38.7ms | 69.6ms | 4.1ms |
| 200 accepted | 121,254 | 0.2ms | 2.2ms | 14.0ms | 23.3ms | 38.7ms | 69.6ms | 4.1ms |

**All requests** is the honest headline: it includes failed and timed-out requests at their measured latency. Reading the accepted-only row instead caps the tail at the 10s timeout and lets percentiles *improve* under load as the slowest requests drop out of the set.

## Headroom

**Ceiling used: 14,000 req/s — the general throughput ceiling.**

This run drives GET /api/v1/health — the cheap request-handling path through nginx and the three Go replicas, with no Argon2id hashing and no Postgres round trip. The login-specific ~105 req/s ceiling does not apply, because no login was issued: that ceiling is set by Argon2id saturating all cores, and nothing in this run hashes a password.

| | |
|---|---|
| Achieved | 12,125.4 req/s |
| Ceiling | 14,000 req/s |
| Utilization | **86.6%** |
| 50%-of-capacity target | 7,000 req/s |
| Remaining headroom | 1,874.6 req/s |

> **OVER 50% OF CAPACITY.** At 12,125.4 req/s this run uses 86.6% of the ceiling, past the 50% rule-of-thumb target of 7,000 req/s. Only 1,874.6 req/s of headroom is left, so a spike of much more than 15.5% has nowhere to go — and losing one of the three replicas would put the remaining two over the line.

## Error rate

| | |
|---|---|
| Successful (200) | 121,254 / 121,254 |
| Success rate | **100.0000%** |
| Failed | 0 (0.0000%) |
| — transport errors | 0 |
| — non-200 responses | 0 |
| Availability | no failures in 121,254 requests — bounds availability at >= 5.1 nines, which is the most a sample this size can show |

A request counts as failed if it errored in transport **or** did not answer 200. The two are separated because a 502 and a timeout are both failures with entirely different causes.

**Status codes:** `200`=121,254

## Saturation

Cores `0-7` — 8 cores allocated, so a fully saturated set reads **800%** in docker terms (100% is one busy core). The load generator is pinned outside this set and is excluded.

| Container | avg CPU% | peak CPU% | polls |
|---|---|---|---|
| `secure-auth-gateway-nginx-1` | 327.8 | 356.5 | 40 |
| `secure-auth-gateway-app1-1` | 91.2 | 104.5 | 40 |
| `secure-auth-gateway-app3-1` | 90.9 | 96.5 | 40 |
| `secure-auth-gateway-app2-1` | 89.6 | 98.5 | 40 |
| `secure-auth-gateway-redis-1` | 1.9 | 4.0 | 40 |
| `secure-auth-gateway-postgres-1` | 1.8 | 8.6 | 40 |
| **Combined** | **603.2** | **644.5** | 40 |

| | |
|---|---|
| Saturation of 8 cores (avg) | **75.4%** |
| Saturation of 8 cores (peak) | **80.6%** |
| Polls counted | 40 |
| Effective poll interval | 250ms |

Samples are intersected with the measured window `2026-08-19T02:56:33Z`–`2026-08-19T02:56:43Z`, so saturation covers exactly the seconds the throughput and latency figures came from.

> The cores are working hard but not pinned. There is probably some throughput left, and the next concurrency step up should show where it runs out.

---

_Generated by `cmd/loadreport` from `/report/loadtest_50_20260819T025554Z.json`. See `docs/LOADTEST.md` for how to read this._
