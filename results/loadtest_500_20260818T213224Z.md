# Load test — concurrency 500

| | |
|---|---|
| Run started (UTC) | 2026-08-18T21:32:59Z |
| Concurrency | 500 |
| Target | `/api/v1/health` via `http://nginx:80` |
| Measured window | 15s (5s warmup discarded) |
| Load model | closed loop, 500 virtual users, think=0s |
| Per-request timeout | 10s |
| Phase reported | `health` |
| Cores under test | 0-7 (8 allocated) |

## Summary

| Concurrency | Throughput (req/s) | p99 latency | Error rate | Total requests | Failed requests |
|---|---|---|---|---|---|
| 500 | 9,258.7 | 177.5ms | 0.0000% | 138,880 | 0 |

Throughput counts every request that completed inside the measured window; 9,258.7 req/s of that answered 200. p99 is over **all** requests, failures included.

## Tail latency

| Distribution | n | min | p50 | p95 | p99 | p99.9 | max | mean |
|---|---|---|---|---|---|---|---|---|
| All requests | 138,880 | 0.3ms | 45.4ms | 126.7ms | 177.5ms | 225.9ms | 292.1ms | 53.9ms |
| 200 accepted | 138,880 | 0.3ms | 45.4ms | 126.7ms | 177.5ms | 225.9ms | 292.1ms | 53.9ms |

**All requests** is the honest headline: it includes failed and timed-out requests at their measured latency. Reading the accepted-only row instead caps the tail at the 10s timeout and lets percentiles *improve* under load as the slowest requests drop out of the set.

## Headroom

**Ceiling used: 14,000 req/s — the general throughput ceiling.**

This run drives GET /api/v1/health — the cheap request-handling path through nginx and the three Go replicas, with no Argon2id hashing and no Postgres round trip. The login-specific ~105 req/s ceiling does not apply, because no login was issued: that ceiling is set by Argon2id saturating all cores, and nothing in this run hashes a password.

| | |
|---|---|
| Achieved | 9,258.7 req/s |
| Ceiling | 14,000 req/s |
| Utilization | **66.1%** |
| 50%-of-capacity target | 7,000 req/s |
| Remaining headroom | 4,741.3 req/s |

> **OVER 50% OF CAPACITY.** At 9,258.7 req/s this run uses 66.1% of the ceiling, past the 50% rule-of-thumb target of 7,000 req/s. Only 4,741.3 req/s of headroom is left, so a spike of much more than 51.2% has nowhere to go — and losing one of the three replicas would put the remaining two over the line.

## Error rate

| | |
|---|---|
| Successful (200) | 138,880 / 138,880 |
| Success rate | **100.0000%** |
| Failed | 0 (0.0000%) |
| — transport errors | 0 |
| — non-200 responses | 0 |
| Availability | no failures in 138,880 requests — bounds availability at >= 5.1 nines, which is the most a sample this size can show |

A request counts as failed if it errored in transport **or** did not answer 200. The two are separated because a 502 and a timeout are both failures with entirely different causes.

**Status codes:** `200`=138,880

## Saturation

Cores `0-7` — 8 cores allocated, so a fully saturated set reads **800%** in docker terms (100% is one busy core). The load generator is pinned outside this set and is excluded.

| Container | avg CPU% | peak CPU% | polls |
|---|---|---|---|
| `secure-auth-gateway-nginx-1` | 239.0 | 312.0 | 61 |
| `secure-auth-gateway-app2-1` | 92.8 | 125.2 | 61 |
| `secure-auth-gateway-app1-1` | 91.0 | 109.8 | 61 |
| `secure-auth-gateway-app3-1` | 89.6 | 112.8 | 61 |
| `secure-auth-gateway-redis-1` | 2.0 | 5.0 | 61 |
| `secure-auth-gateway-postgres-1` | 1.6 | 4.5 | 61 |
| **Combined** | **516.0** | **607.4** | 61 |

| | |
|---|---|
| Saturation of 8 cores (avg) | **64.5%** |
| Saturation of 8 cores (peak) | **75.9%** |
| Polls counted | 61 |
| Effective poll interval | 250ms |

Samples are intersected with the measured window `2026-08-18T21:32:59Z`–`2026-08-18T21:33:14Z`, so saturation covers exactly the seconds the throughput and latency figures came from.

> The cores are working hard but not pinned. There is probably some throughput left, and the next concurrency step up should show where it runs out.

---

_Generated by `cmd/loadreport` from `/report/loadtest_500_20260818T213224Z.json`. See `docs/LOADTEST.md` for how to read this._
